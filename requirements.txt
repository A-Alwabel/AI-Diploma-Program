# AI Diploma - student environment baseline
#
# Install into a fresh virtual environment:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install --upgrade pip
#   pip install -r requirements.txt
#
# NOTE: TensorFlow is NOT in this file. TensorFlow has no wheel for this
# venv's Python, so the TensorFlow/Keras notebooks (Courses 01 and 08) run
# in a separate Python 3.13 environment registered as the "tfenv" Jupyter
# kernel. See docs/SETUP_GUIDE.md.

# --- Core scientific stack (used in every course) ---
numpy
pandas
matplotlib
seaborn
scikit-learn

# --- Jupyter / notebook tooling ---
jupyter
ipykernel
ipywidgets
nbformat
nbconvert
nbclient

# --- Data I/O and visualization ---
openpyxl                      # Excel files (pandas read_excel/to_excel)
plotly                        # interactive charts (Course 05)
dask[dataframe]               # scalable dataframes (Course 05)

# --- Knowledge representation (Course 01) ---
SPARQLWrapper
rdflib

# --- Ethics, fairness, explainability (Course 06) ---
fairlearn
shap
lime
cryptography                  # privacy/security labs (Courses 06, 11)

# --- Reinforcement learning (Course 09) ---
gymnasium[classic-control]
pygame                        # environment rendering
imageio                       # saving episode frames/GIFs

# --- Deep learning and NLP (Courses 07, 08, 10, 11) ---
torch
torchvision
transformers                  # Hugging Face (Courses 07, 10)
nltk                          # classical NLP (Course 07)
spacy                         # NLP pipelines (Course 07)
opencv-python                 # image processing (Course 08)

# --- Deployment / MLOps (Course 11) ---
mlflow
fastapi
uvicorn
flask
onnx
onnxruntime
joblib                        # model persistence
boto3                         # optional: Course 11 cloud lab (AWS)

# --- Optional extras ---
xgboost                       # Course 04 boosting lab (notebook falls back to sklearn if missing)
